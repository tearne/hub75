# Use the mingw-w64 GCC as the linker when cross-compiling to Windows.
# Install on Debian/Ubuntu/Pi OS with: sudo apt install mingw-w64
[target.x86_64-pc-windows-gnu]
linker = "x86_64-w64-mingw32-gcc"
